#!/usr/bin/env python2
# -*- coding: utf-8 -*-
"""
Created on Fri Oct 26 10:55:15 2018

@author: elisabeth
"""

#1 UW vs MSU Plot 
#open data file and read as table
import pandas as pd
data=pd.read_table("/Users/elisabeth/Desktop/BioComputing/biocomputing_exercise8/UWvMSU_1-22-13.txt", header=0, sep='\t')
print(data)

#set original scores to O
start_UW = 0
start_MSU = 0

#generate dataframe- cumulative score for each team
#record after each team scores 
#for loop- if UW, if MSU- cumulative score
for i in range(0,len(data)):
    if data.team.iloc[i] == 'MSU':
        count = data.score[i]
        score_MSU= data.score[i] + start_MSU
        output.loc = [time, score_UW, score_MSU]
    elif data.team.iloc[i] == 'UW':
        count = data.score[i]
        score_UW = data.score[i] + start_UW
        output.loc = [time, score_UW, score_MSU]
        

#unable to import matplot lib due to library error
#says my libarry version is incompatible with mat plot lib 
#this would be the code to plot if my computer allowed me to 
import matplotlib.pyplot as plt 
plt.plot(time, score_UW, 'r-', time, score_MSU,'g-')




#2- guess my number game
#generate random number from computer
#output is the random number the computer came up with 
import numpy 
data = range (0,100)
output = numpy.random.choice(data)
print(output)
#compares guess to random nubmer generated by computer 
guess = raw_input()
if guess > output:
    print("higher")
else:
    print("lower")